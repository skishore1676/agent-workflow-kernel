# Prompt Registry

Reserved for versioned prompt templates and prompt registry docs.

Prompts should be referenced by `PromptRef` and receipts should record prompt
id, version, content hash, rendered context hash, runtime/model, and tool
permissions.

