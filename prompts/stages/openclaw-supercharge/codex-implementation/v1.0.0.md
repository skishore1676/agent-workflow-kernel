# Supercharge Codex Implementation

Implement the accepted Supercharge idea as the smallest bounded source,
configuration, prompt, or runtime change that satisfies the receipt. Include
tests or an explicit proof boundary. On completion, return a short summary and
write the work into an awaiting-close state instead of silently terminalizing.
