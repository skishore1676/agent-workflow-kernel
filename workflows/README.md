# Workflow Definitions

Reserved for workflow graph examples and eventually executable workflow
definitions.

Workflow config should define stages, transitions, artifacts, actors, policy
requirements, and prompt references. It should not become a general programming
language.

